version: "3.8"

services:
  sonarqube:
    image: sonarqube:10-community
    container_name: sonarqube-imobi-facil
    depends_on:
      - db
    environment:
      SONAR_JDBC_URL: jdbc:postgresql://db:5432/sonar
      SONAR_JDBC_USERNAME: sonar
      SONAR_JDBC_PASSWORD: sonar
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_extensions:/opt/sonarqube/extensions
      - sonarqube_logs:/opt/sonarqube/logs
    ports:
      - "9000:9000"
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    restart: unless-stopped

  db:
    image: postgres:15-alpine
    container_name: postgres-sonar-imobi-facil
    environment:
      POSTGRES_USER: sonar
      POSTGRES_PASSWORD: sonar
      POSTGRES_DB: sonar
    volumes:
      - postgresql_data:/var/lib/postgresql/data
    restart: unless-stopped

  node-sonar:
    build:
      context: .
      dockerfile: Dockerfile # seu Dockerfile enxuto para SonarQube Scanner
    container_name: node-sonar
    depends_on:
      - sonarqube
    volumes:
      - ./:/app
    environment:
      SONAR_HOST_URL: http://sonarqube:9000
      SONAR_TOKEN: ${SONAR_TOKEN}
    working_dir: /app
    command: ["npx", "sonar-scanner"]

volumes:
  sonarqube_data:
  sonarqube_extensions:
  sonarqube_logs:
  postgresql_data:
